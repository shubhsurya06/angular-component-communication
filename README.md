# Angular Component Communication

This repository demonstrates the **different ways components communicate in Angular**.  
It serves as a practical reference for developers to understand **when and how to use each communication technique**.

---

## 🚀 Topics Covered

This repository includes examples for:

- `@Input()` – Parent to child data binding
- `@Output()` & `EventEmitter` – Child to parent communication
- `@ViewChild` – Accessing child component or DOM element
- `@ViewChildren` – Accessing multiple child components or elements
- `@ContentChild` – Accessing projected content
- `@ContentChildren` – Accessing multiple projected elements
- Content Projection (`<ng-content>`)
- Service-based communication (shared services)
- Signals-based communication (modern Angular)
- Change Detection basics (`markForCheck`, `detectChanges`)

---

## 🧠 Why This Repository?

Angular offers **multiple communication patterns**, each suitable for different scenarios.  
This repo helps you:

- Understand **when to use which approach**
- Avoid anti-patterns
- Write clean and maintainable Angular code
- Learn modern Angular practices (Signals, Standalone components)

---
